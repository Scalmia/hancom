# 클로드 코드 설치 과정
1. https://code.claude.com/docs/ko/quickstart 에 접속
2. Native install 에 적혀 있는 win cmd 버전 설치 명령어 복사 `curl -fsSL https://claude.ai/install.cmd -o install.cmd && install.cmd && del install.cmd`
3. CMD 입력
4. 나온 문구에서 bin 경로를 windows 환경변수 유저 설정에 입력
5. CMD 에서 claude 실행 후 계정 인증 진행.